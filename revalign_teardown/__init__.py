"""revalign-teardown — a visual wall of real internal teardown photos of
barely-released hardware, pulled from FCC equipment-authorization filings.

The pipeline (this package) is stdlib-first and builds a JSON index that the
self-contained web gallery in ``web/`` renders. Every card is self-verifiable
against the live FCC record.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
